<%= image_tag "power_up.png", class: "logo smaller" %>

<% if flash[:notice] %>
  <p class="form-errors"><%= flash[:notice] %></p>
<% end %>

<div id="inner-container">
  <% if @all_lockers_full %>
    <div class="text-box orange">
      <p>SORRY, ALL LOCKERS ARE CURRENTLY FULL</p>
    </div>

  <% else %>
    <%= form_for @rental, html: {id: "new-rental-form"} do |f| %>
      <%= f.hidden_field :creation_device_id, value: @device_id %>
      <%= f.hidden_field :large, value: @large %>
      <%= f.text_field :last_name, placeholder: "LAST NAME", class: "text-input line-2" %>
      <%= f.text_field :phone_number, placeholder: "PHONE NUMBER", class: "text-input line-3",
          type:"number", min: "0", inputmode:"numeric", pattern:"[0-9]*" %>
      <div class="text-input line-4 checkbox-container">
        <%= f.check_box :terms %>
        <p>IF YOU AGREE BY CHECKING THIS BOX YOUR PERSONAL INFORMATION WILL BE USED SOLELY TO RESERVE YOUR CHARGING STATION LOCKER AND WILL NOT BE STORED AFTER YOUR USE.</p>
      </div>
      <%= f.submit "submit", id: "new-submit-button", class: "hidden-submit" %>
    <% end %>
  <% end %>
</div>

<div id="button-container">
  <%= link_to "BACK", size_rentals_path, class: "btn left small" %>
  <% unless @all_lockers_full %>
    <label for="new-submit-button" class="btn center large green">ENTER</label>
  <% end %>

</div>
